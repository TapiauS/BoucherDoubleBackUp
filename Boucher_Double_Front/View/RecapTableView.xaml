<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Boucher_Double_Front.View.RecapTableView"
             Title="RecapTableView"
             BackgroundColor="{DynamicResource BackgroundColor}">
    <VerticalStackLayout>
        
        <Picker ItemsSource="{Binding AllEvents}" SelectedIndexChanged="OnEventSelection" x:Name="EventPicker" ItemDisplayBinding="{Binding Name}"/>
        <ScrollView Orientation="Horizontal">
            <Grid x:Name="table"  >

            </Grid>
        </ScrollView>
    </VerticalStackLayout>
</ContentPage>